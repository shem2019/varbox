# Core runtime deps validated in the local Python 3.13 environment.
# Apple Silicon should use the default PyTorch wheels so YOLOv8 can run on MPS.

# Numeric stack
numpy==2.4.3
scipy==1.17.1
mediapipe==0.10.32
# Vision & pose
opencv-python==4.13.0.92

fpdf2==2.8.7
PySide6-Essentials==6.10.2
protobuf==7.34.0

torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
ultralytics==8.4.22


# Desktop UI (our PySide6 GUI)
# Only needed if you build a Windows .exe locally
