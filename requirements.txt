# Core runtime deps (Windows, CPU-only). Tested on Python 3.10–3.11.

# Numeric stack
numpy
scipy
mediapipe
# Vision & pose
opencv-python

fpdf
  # mediapipe works best with protobuf 3.20.x on Windows
PySide6-Essentials==6.7.2
protobuf

# CPU-only PyTorch (Linux) – prefer these wheels
-f https://download.pytorch.org/whl/cpu/torch_stable.html

torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0


# Desktop UI (our PySide6 GUI)
# Only needed if you build a Windows .exe locally
