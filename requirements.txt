# Core runtime deps (Windows, CPU-only). Tested on Python 3.10–3.11.

# Numeric stack
numpy==1.24.4
scipy==1.10.1

# Vision & pose
opencv-python==4.9.0.80
mediapipe==0.10.14
protobuf==3.20.3   # mediapipe works best with protobuf 3.20.x on Windows

# PDF scorecard
fpdf==1.7.2

# Desktop UI (our PySide6 GUI)
PySide6==6.7.2
# Only needed if you build a Windows .exe locally
pyinstaller==6.6.0
